<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:background="@drawable/x_card_background"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:ignore="UselessParent">

        <LinearLayout
            android:id="@+id/xCardGradient"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@drawable/x_card_gradient_bottom_dialog"
            android:orientation="horizontal" />

        <ImageView
            android:id="@+id/xCardPic"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="17dp"
            android:src="@drawable/x_logo"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/xCardTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_marginBottom="30dp"
            android:layout_marginStart="10dp"
            android:textDirection="locale"
            android:textColor="@color/x_card_title_color"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="XTitle"/>

        <TextView
            android:id="@+id/xCardSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_marginBottom="10dp"
            android:layout_marginStart="10dp"
            android:textDirection="locale"
            android:textSize="12sp"
            android:text="XSummary"/>

    </FrameLayout>

</LinearLayout>