<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CoolX.Theme" parent="Theme.AppCompat.DayNight">
        <item name="windowNoTitle">false</item>
        <item name="windowActionBar">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowAnimationStyle">@style/CoolX.Animation.Activity</item>

        <!-- EditText -->
        <item name="editTextStyle">@style/CoolX.Widget.AppCompat.EditText</item>
        <!-- For Platform EditText -->
        <item name="android:editTextStyle">@style/CoolX.Widget.AppCompat.EditText</item>

        <!-- AlertDialog -->
        <item name="alertDialogTheme">@style/CoolX.ThemeOverlay.AppCompat.Dialog.Alert</item>
        <item name="alertDialogStyle">@style/CoolX.AlertDialog.AppCompat</item>
    </style>

    <!-- CoolX Theme, NoActionBar -->
    <style name="CoolX.Theme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowAnimationStyle">@style/CoolX.Animation.Activity</item>

        <!-- AlertDialog -->
        <item name="alertDialogTheme">@style/CoolX.ThemeOverlay.AppCompat.Dialog.Alert</item>
        <item name="alertDialogStyle">@style/CoolX.AlertDialog.AppCompat</item>
    </style>

</resources>